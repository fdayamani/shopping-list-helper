version 'unspecified'

apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile project(':shopping-list-helper-application')
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
